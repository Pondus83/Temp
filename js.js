var temp = 20; // Skal være inden for -20 til 30
var varmeFelt = document.querySelectorAll("#varme .st1");
var kuldeFelt = document.querySelectorAll("#kulde .st0");

if (temp >= 0) {
    for (i = 0; i < temp; i++) {
        varmeFelt[i].classList.add(this.add
            
            // getAttribute("data-add"));
    }
}